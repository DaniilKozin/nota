# Быстрое исправление Audio - 14 января 2026

## Что было не так
Nota меняла системный default input device программно, что ломало Zoom/Teams - собеседник переставал слышать вас.

## Что исправлено
✅ Nota больше НЕ меняет системный default input device  
✅ Использует тот device, который установлен в System Settings  
✅ Zoom/Teams работают нормально  

## Как настроить (один раз)

### 1. Создать Aggregate Device
```
Audio MIDI Setup → + → Create Aggregate Device
Включить: ✓ Built-in Microphone
          ✓ BlackHole 2ch
Clock Source: Built-in Microphone
```

### 2. Установить как системный input
```
System Settings → Sound → Input
Выбрать: Aggregate Device
```

### 3. Настроить Zoom/Teams
```
Settings → Audio
Input: Aggregate Device (ваш голос)
Output: BlackHole 2ch (звук собеседника)
```

### 4. Запустить Nota
```
Dashboard → Record
Nota автоматически использует системный input
```

## Проверка

### Zoom/Teams должен:
✅ Слышать вас (Input = Aggregate Device с микрофоном)  
✅ Отправлять звук в BlackHole (Output = BlackHole 2ch)  

### Nota должна:
✅ Захватывать микрофон (из Aggregate Device)  
✅ Захватывать BlackHole (из Aggregate Device)  
✅ Показывать транскрипцию обоих голосов  

### В консоли Nota:
```
🎧 Using system default audio input device
🎧 Current input device: Aggregate Device (ID: ...)
🎧 Recording from: Aggregate Device
```

## Если не работает

### Собеседник не слышит меня:
1. ✅ Проверить Zoom Input = Aggregate Device (НЕ BlackHole!)
2. ✅ Проверить что микрофон включен в Aggregate Device
3. ✅ Проверить System Settings → Sound → Input = Aggregate Device

### Nota не слышит собеседника:
1. ✅ Проверить Zoom Output = BlackHole 2ch
2. ✅ Проверить что BlackHole включен в Aggregate Device
3. ✅ Проверить System Settings → Sound → Input = Aggregate Device

### Не слышу собеседника в наушниках:
1. ✅ Создать Multi-Output Device (BlackHole + наушники)
2. ✅ Zoom Output = Multi-Output Device

## Важно

⚠️ **Nota НЕ меняет системный audio device!**  
Вы устанавливаете Aggregate Device в System Settings один раз, и все приложения его используют.

✅ **Преимущества:**
- Zoom/Teams работают нормально
- Собеседник слышит вас
- Nota захватывает оба голоса
- Не нужно переключать devices каждый раз

---

**Статус:** ✅ Исправлено  
**Дата:** 14 января 2026  
