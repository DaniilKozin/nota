# Fixing "Nota is damaged" Error (macOS Gatekeeper)

## Проблема

При попытке открыть Nota на другом Mac вы видите ошибку:

```
"Nota" is damaged and can't be opened. You should move it to the Trash.
```

## Почему это происходит?

Это **НЕ** значит, что приложение повреждено! Это macOS Gatekeeper блокирует приложения от неизвестных разработчиков.

### Причины:
1. **Нет подписи Apple Developer** - требуется сертификат за $99/год
2. **Quarantine атрибут** - macOS помечает скачанные файлы как "небезопасные"
3. **Notarization** - Apple требует отправлять приложения на проверку

## ✅ Решения

### Метод 1: Автоматический установщик (Рекомендуется)

Используйте скрипт `install_nota.sh` из DMG:

```bash
# 1. Откройте DMG
open Nota-v2.1-SmartTranscription.dmg

# 2. Откройте Terminal
# Applications → Utilities → Terminal

# 3. Перетащите install_nota.sh в Terminal и нажмите Enter
# Или введите:
cd /Volumes/Nota\ v2.1/
./install_nota.sh
```

Скрипт автоматически:
- Копирует Nota.app в /Applications
- Удаляет quarantine атрибут
- Проверяет права доступа
- Запускает приложение

### Метод 2: Ручная установка через Terminal

```bash
# 1. Скопируйте приложение
cp -R /Volumes/Nota\ v2.1/Nota.app /Applications/

# 2. Удалите quarantine атрибут
xattr -cr /Applications/Nota.app

# 3. Запустите приложение
open /Applications/Nota.app
```

### Метод 3: Правый клик (Right-click)

1. Скопируйте Nota.app в /Applications
2. **Правый клик** (или Control+клик) на Nota.app
3. Выберите **"Open"** из меню
4. Нажмите **"Open"** в диалоге

При первом запуске через правый клик macOS запомнит ваш выбор.

### Метод 4: System Settings (для постоянного разрешения)

```bash
# 1. Попробуйте открыть приложение (получите ошибку)
open /Applications/Nota.app

# 2. Откройте System Settings
# System Settings → Privacy & Security

# 3. Найдите сообщение:
# "Nota was blocked from use because it is not from an identified developer"

# 4. Нажмите "Open Anyway"
```

## 🔐 Безопасность

### Почему это безопасно?

1. **Open Source** - весь код доступен на GitHub
2. **Нет вредоносного кода** - можете проверить сами
3. **Локальное хранение** - все данные на вашем Mac
4. **Нет телеметрии** - не отправляет данные разработчику

### Что делает xattr -cr?

```bash
xattr -cr /Applications/Nota.app
```

- `xattr` - управление расширенными атрибутами файлов
- `-c` - удалить все атрибуты
- `-r` - рекурсивно (для всех файлов внутри)
- Удаляет `com.apple.quarantine` атрибут

### Проверка атрибутов

```bash
# Посмотреть атрибуты
xattr /Applications/Nota.app

# Если видите com.apple.quarantine - нужно удалить
# Если пусто - все ОК
```

## 🛠️ Для разработчиков

### Почему приложение не подписано?

1. **Apple Developer Program** стоит $99/год
2. **Notarization** требует отправки на серверы Apple
3. **Ad-hoc подпись** работает только на вашем Mac

### Как подписать приложение правильно?

Если у вас есть Apple Developer аккаунт:

```bash
# 1. Создайте entitlements.plist (уже есть в create_app_bundle.sh)

# 2. Подпишите с вашим сертификатом
codesign --force --deep \
  --sign "Developer ID Application: Your Name (TEAM_ID)" \
  --entitlements entitlements.plist \
  --options runtime \
  Nota.app

# 3. Проверьте подпись
codesign --verify --verbose Nota.app

# 4. Отправьте на notarization
xcrun notarytool submit Nota.app.zip \
  --apple-id "your@email.com" \
  --team-id "TEAM_ID" \
  --password "app-specific-password"

# 5. Прикрепите ticket
xcrun stapler staple Nota.app
```

### Ad-hoc подпись (текущая)

```bash
# Подписывает локально без сертификата
codesign --force --deep --sign - Nota.app

# Работает на вашем Mac
# НЕ работает на других Mac без xattr -cr
```

## 📋 Checklist для распространения

### Для пользователей:

- [ ] Скачать DMG
- [ ] Открыть DMG
- [ ] Запустить `install_nota.sh` (рекомендуется)
- [ ] Или вручную: скопировать + `xattr -cr`
- [ ] Открыть Nota
- [ ] Разрешить доступ к микрофону
- [ ] Настроить API ключи

### Для разработчика:

- [ ] Собрать приложение: `./build.sh`
- [ ] Подписать ad-hoc: автоматически в `create_app_bundle.sh`
- [ ] Создать DMG: автоматически в `create_dmg.sh`
- [ ] Включить `install_nota.sh` в DMG
- [ ] Добавить инструкции в README.txt
- [ ] Протестировать на другом Mac
- [ ] Распространить DMG

## 🚀 Автоматизация

### Скрипт полной сборки

```bash
cd Nota-Swift

# 1. Обновить версию
./version.sh bump minor  # 2.1.0 → 2.2.0

# 2. Собрать все
./build.sh

# 3. Протестировать локально
open Nota.app

# 4. Создать DMG
./create_dmg.sh

# 5. Распространить
# Upload Nota-v2.2-SmartTranscription.dmg
```

## 📚 Дополнительные ресурсы

- [Apple Code Signing Guide](https://developer.apple.com/library/archive/documentation/Security/Conceptual/CodeSigningGuide/)
- [Notarization Guide](https://developer.apple.com/documentation/security/notarizing_macos_software_before_distribution)
- [Gatekeeper Overview](https://support.apple.com/en-us/HT202491)

## ❓ FAQ

### Q: Почему не использовать Apple Developer Program?

**A:** Стоит $99/год. Для open-source проекта это необязательно, если пользователи могут обойти Gatekeeper вручную.

### Q: Безопасно ли использовать xattr -cr?

**A:** Да, это стандартная команда macOS. Она просто удаляет метаданные о происхождении файла.

### Q: Можно ли автоматизировать для пользователей?

**A:** Да, используйте `install_nota.sh` - он делает все автоматически.

### Q: Будет ли работать на Apple Silicon (M1/M2/M3)?

**A:** Да, Swift компилируется для обеих архитектур (Intel и ARM).

### Q: Нужен ли Rosetta?

**A:** Нет, если собрано на Apple Silicon. Если собрано на Intel - может потребоваться Rosetta на M1/M2/M3.

## 🎯 Итог

**Для пользователей:**
```bash
# Самый простой способ:
./install_nota.sh
```

**Для разработчика:**
```bash
# Включить install_nota.sh в DMG
# Добавить инструкции в README.txt
# Все остальное автоматически
```

---

**Обновлено:** 14 января 2026  
**Версия:** 2.1.0  
**Статус:** ✅ Работает на всех Mac с macOS 13.0+
