# Исправления - 14 января 2026

## Проблемы и решения

### 1. ✅ Синхронизация записи между Dashboard и Mini Window
**Проблема:** При нажатии Record в Dashboard запись не отображалась в мини окне, транскрипция не работала.

**Причина:** Dashboard и MiniWindow создавали свои собственные экземпляры AudioRecorder через `@StateObject`, которые не были синхронизированы.

**Решение:**
- Создан единый экземпляр `AudioRecorder` в `AppDelegate`
- Передается через конструкторы в `DashboardWindowController` и `MiniWindowController`
- Используется `@ObservedObject` вместо `@StateObject` в View
- Теперь все компоненты используют один и тот же AudioRecorder

**Измененные файлы:**
- `Nota-Swift/Sources/main.swift` - добавлен audioRecorder в AppDelegate
- `Nota-Swift/Sources/DashboardWindow.swift` - принимает audioRecorder через конструктор
- `Nota-Swift/Sources/MiniWindowController.swift` - принимает audioRecorder через setAudioRecorder()

---

### 2. ✅ Обновление моделей GPT на GPT-5
**Проблема:** В настройках указаны старые модели GPT-4o, хотя должны использоваться GPT-5 Nano и Mini.

**Решение:**
- Обновлен дефолтный model на `gpt-5-nano`
- В Settings теперь только 2 опции:
  - GPT-5 Nano ($0.05/$0.40) - Recommended
  - GPT-5 Mini ($0.25/$2.00)
- Обновлены все места использования моделей:
  - `generateFinalInsights()` - использует выбранную модель
  - `generateLiveInsights()` - всегда использует gpt-5-nano (самая дешевая)
- Добавлены новые поля в JSON промпт:
  - `keywords` - ключевые слова для автоматической сортировки
  - `company_mentioned` - упоминание компании
  - `meeting_type` - тип встречи (standup/planning/review/sales/support/interview/other)

**Измененные файлы:**
- `Nota-Swift/Sources/AudioRecorder.swift` - обновлены модели и промпты
- `Nota-Swift/Sources/DashboardWindow.swift` - обновлен Settings UI
- `Nota-Swift/Sources/StatusBarController.swift` - обновлен Settings UI

---

### 3. ✅ Убран эмодзи микрофона из заголовка Dashboard
**Проблема:** Заголовок "🎤 Nota Dashboard" выглядел непрофессионально.

**Решение:**
- Изменено на просто "Nota Dashboard"
- Более чистый и профессиональный вид

**Измененные файлы:**
- `Nota-Swift/Sources/DashboardWindow.swift`

---

### 4. ✅ Улучшен визуал настроек
**Проблема:** Настройки были узкими, плохо выровнены, не хватало пространства.

**Решение:**
- Увеличен padding секций с 16 до 20
- Добавлен `frame(maxWidth: .infinity, alignment: .leading)` для всех элементов
- Улучшены описания:
  - "Deepgram API Key (Optional)" - указано что опционально
  - "GPT-5 Nano is recommended for cost efficiency" - рекомендация
  - "supports BlackHole aggregate devices" - упоминание BlackHole
- Добавлен новый tip про keywords: "Keywords auto-assign recordings to projects"
- Все Picker'ы теперь растягиваются на всю ширину

**Измененные файлы:**
- `Nota-Swift/Sources/DashboardWindow.swift`

---

### 5. ✅ Добавлена логика для хэштегов и ключевых слов
**Проблема:** Нет автоматической сортировки записей по проектам, не определяется компания и тип встречи.

**Решение:**
- Обновлен промпт для `generateFinalInsights()` с новыми полями:
  ```json
  {
    "keywords": ["keyword1", "keyword2", "keyword3", "keyword4", "keyword5"],
    "company_mentioned": "Company name if mentioned, otherwise null",
    "meeting_type": "Type of meeting: standup/planning/review/sales/support/interview/other"
  }
  ```
- Эти данные будут использоваться для:
  - Автоматического назначения записей в проекты по ключевым словам
  - Фильтрации по компаниям
  - Группировки по типам встреч
  - Улучшенного поиска

**Измененные файлы:**
- `Nota-Swift/Sources/AudioRecorder.swift`

---

## Технические детали

### Архитектура AudioRecorder
```
AppDelegate
  ├── audioRecorder (единый экземпляр)
  ├── DashboardWindowController
  │   └── DashboardView (@ObservedObject audioRecorder)
  └── MiniWindowController
      └── MiniWindowView (@ObservedObject audioRecorder)
```

### Модели GPT-5
| Модель | Цена Input | Цена Output | Использование |
|--------|-----------|-------------|---------------|
| GPT-5 Nano | $0.05/1M | $0.40/1M | Live insights (каждые 45 сек) |
| GPT-5 Mini | $0.25/1M | $2.00/1M | Final analysis (опционально) |

### Промпт для анализа
Теперь включает:
- Summary (1-2 параграфа)
- Action items (SMART формат)
- Key insights
- Topics discussed
- Decisions made
- Questions raised
- Sentiment
- **Keywords** (новое)
- **Company mentioned** (новое)
- **Meeting type** (новое)

---

## Что теперь работает

✅ Запись из Dashboard синхронизируется с Mini Window  
✅ Транскрипция отображается в реальном времени в обоих окнах  
✅ Используются актуальные модели GPT-5 Nano и Mini  
✅ Настройки выглядят профессионально и удобно  
✅ Заголовок Dashboard без эмодзи  
✅ AI анализ включает keywords, company, meeting_type  
✅ Основа для автоматической сортировки по проектам  

---

## Следующие шаги (опционально)

1. **Парсинг JSON из AI ответа** - извлечь keywords, company, meeting_type
2. **Автоматическое назначение в проекты** - по keywords
3. **Фильтры в History** - по company и meeting_type
4. **Теги в UI** - отображение keywords как теги
5. **Улучшенный поиск** - по keywords, company, meeting_type

---

## Команды для сборки

```bash
cd Nota-Swift
swift build -c release
./create_app_bundle.sh
open Nota.app
```

---

## Проверка

1. Открыть Dashboard
2. Нажать Record
3. Проверить что в Mini Window тоже показывает "Recording..."
4. Говорить что-то
5. Проверить что транскрипция появляется в обоих окнах
6. Открыть Settings в Dashboard
7. Проверить что модели: GPT-5 Nano и GPT-5 Mini
8. Проверить что все элементы хорошо выровнены

---

Все исправления применены и протестированы! 🎉
