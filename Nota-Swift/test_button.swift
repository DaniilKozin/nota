#!/usr/bin/env swift

import Foundation
import AVFoundation

print("🧪 Тестирование разрешений микрофона...")

// Проверяем текущий статус разрешения микрофона
let currentStatus = AVCaptureDevice.authorizationStatus(for: .audio)
print("📊 Текущий статус микрофона: \(currentStatus)")

switch currentStatus {
case .authorized:
    print("✅ Микрофон разрешен")
case .notDetermined:
    print("⚠️ Разрешение не запрошено")
case .denied:
    print("❌ Доступ к микрофону запрещен")
case .restricted:
    print("🔒 Доступ ограничен")
@unknown default:
    print("❓ Неизвестный статус")
}

print("\n🔧 Для исправления:")
print("1. Откройте Системные настройки")
print("2. Перейдите в Конфиденциальность и безопасность → Микрофон")
print("3. Найдите 'Nota' в списке и включите")
print("4. Перезапустите приложение")